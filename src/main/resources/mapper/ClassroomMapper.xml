<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyf.courseschedulingsystem.mapper.ClassroomMapper">
    <select id="list" resultType="com.zyf.courseschedulingsystem.vo.classroom.ClassroomVO">
        select * from classroom where is_del = 0 order by id;
    </select>
</mapper>
